# Scenario Layer

`frontend/scenario` owns runtime target and safety configuration.

Current status:
- Scenario state dataclasses are available for runtime payload building.
- UI widgets are placeholders and can be wired incrementally.
