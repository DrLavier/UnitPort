# Unitree SDK2 Adapter

This adapter wraps `models.unitree.UnitreeModel` and exposes the service adapter ABI.

Files:
- `adapter.py`: adapter implementation.
- `mapper.py`: canonical action name mapping.
