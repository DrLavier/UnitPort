[default]
already_running = A training run is already active
no_canvas = No canvas loaded
preparing = Preparing training run (compiling off the UI thread)…
started = Training started: run={run} backend={backend} algo={algo}

[coverage]
blocked.body = Reward coverage is critical — the policy is likely to learn undesired behaviors when the affected phases are active.
blocked.cancel = Cancel
blocked.header = Cannot start training: reward coverage check failed.
blocked.hint = Double-click the badge on the Rewards node to open the Coverage Inspector for suggested fixes.
blocked.proceed = Continue anyway
blocked.title = Reward Coverage Issue

[deploy_coverage]
affected = Affected deploy targets:
body = The trained bundle will be deployable to ONLY a subset of the supported targets, because the robot's MJCF and USD asset tables don't declare the same joint set.
cancel = Cancel
header = Training will produce a bundle with reduced deploy coverage for robot ‘{name}’ (sku={sku}).
hint = To restore both deploy targets: cancel, open the Robot Assets sidebar, repoint the smaller-DOF asset to a matching variant (e.g. for Unitree G1 use scene_with_hands.xml alongside g1.usd), re-Dump, then re-run Play.
miss_mjcf = Roles declared in USD but missing from MJCF: {roles}
miss_usd = Roles declared in MJCF but missing from USD: {roles}
proceed = Proceed anyway (don't waste compute later)
title = Reduced deploy-target coverage

[sim2sim]
body = The PhysX↔MuJoCo cross-engine measurement found dimension(s) whose residual is non-convergent (chaotic): {dims}. Domain randomization cannot cover them, and sim2sim may not faithfully reproduce this regime.
cancel = Cancel
header = Cross-engine residual self-check flagged robot ‘{sku}’.
hint = Proceed records this for the current plant — you won't be asked again unless the robot asset changes.
proceed = Proceed anyway
title = Non-convergent sim2sim dimension

[packages]
add = Add package
bad_weight = package_weight must be a number
edit_rewards = Edit rewards…
enabled = Enabled
gate_missing = {s} (missing)
gate_none = — none —
gated_by = Gated by skill
gated_by.tip = Restrict this package's rewards to a skill's trigger window — the rewards only count while that skill is firing (post-pulse decay). Requires a skill authored in this node's Skill Channels.
hint = Group items into packages. package_weight rebalances a whole package; each package can own an inline reward set. Empty = one implicit default package.
membership = Item membership
name = Name
new_name = Package {n}
preview = Composed reward preview (= export)
preview_empty = No inline package rewards. Items with no package reward keep their wired rewards node.
remove = Remove
title = Training Packages
weight = package_weight

[skills]
add = Add skill
bad_id = every skill needs an id
bad_window = window must be a positive number
enabled = Enabled
hint = Each skill is a discrete policy command channel (a trigger). It enters the policy obs and rides the command contract; a policy must be trained to respond to it. Binding it to a key or button is done per-user in the Controller panel after training — not here.
latch = Latch
latch.hold = hold (while held)
latch.pulse = pulse (one-shot)
name = Name
new_name = skill {n}
preview = Emitted trigger channels
preview_empty = No enabled skills.
remove = Remove
title = Skill Channels
window = Window (s)
window.tip = Post-pulse decay window: the trigger obs + reward gate decay 1→0 over this many seconds. The deploy runtime rebuilds the same window.

