# Readable IR Outputs

Place human-readable IR artifacts generated from Canvas/Compiler here.

Recommended naming:

- `ir_<mission_or_scenario_id>_<date>.yaml`

This folder is review-oriented and may include intermediate files intended for human inspection.
