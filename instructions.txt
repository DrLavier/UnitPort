UnitPort Construction Directive — 执行计划 & 验收清单
=====================================================
Generated: 2026-03-31
Source: knowledge_base/CONSTRUCTION_DIRECTIVE.yaml

目标: 从 "RL训练工具+任务画布" 转型为 "技能编排平台"
核心链路: Mission → SkillManifest → TransitionValidation → BehaviorDispatch

══════════════════════════════════════════════════════
Phase 1: Foundation (A1)
  依赖: 无
  门控: SkillManifest dataclass 可导入且单元测试通过
══════════════════════════════════════════════════════

  Task A1: SkillManifest dataclass                                   [DONE]
    操作:
      - CREATE src/system/skill/__init__.py
      - CREATE src/system/skill/skill_manifest.py
        frozen dataclass: skill_id, skill_name, version, source_type,
        action_space_type, action_dim, control_frequency_hz, action_range,
        required_sensors, observation_space_keys, observation_dim,
        target_robot_family, target_robot_models, joint_mapping,
        precondition (posture, tolerance, velocity, custom_check),
        postcondition (同上),
        inference_backend, model_path, normalize_obs, normalizer_path,
        training_source, description, tags
      - CREATE src/system/skill/manifest_loader.py
        从 bundle 目录的 manifest.yaml 加载 SkillManifest
        v1 manifest 自动迁移 (填充默认 pre/postcondition)
      - MODIFY src/system/policy/manifest_schema.py
        废弃或重定向到 system/skill/
    验收:
      [OK] src/system/skill/ 模块可导入
      [OK] SkillManifest 所有字段与 CONSTRUCTION_DIRECTIVE schema 一致
      [OK] manifest_loader 能加载现有 v1 manifest.yaml (PPO_WALK, SAC_WALK) 并补全默认值
      [OK] py_compile 通过 (4/4 文件)
      [OK] manifest_schema.py 新增 re-export 桥接，现有代码不受影响
      [OK] 现有测试不回归 (167 passed, 1 pre-existing failure)

══════════════════════════════════════════════════════
Phase 2: Validation (A2, A3)
  依赖: Phase 1
  门控: TransitionValidator 通过 mock manifest 单元测试
══════════════════════════════════════════════════════

  Task A2: TransitionValidator                                       [DONE]
    操作:
      - CREATE src/system/skill/transition_validator.py
        输入: 有序 SkillManifest 列表 (DAG拓扑排序)
        输出: TransitionCheckResult 列表 (compatible | needs_transition | incompatible)
        逻辑: 检查相邻对的 posture/velocity/action_space/frequency 兼容性
      - CREATE src/system/skill/transition_result.py
    验收:
      [OK] 相邻 manifest 对的兼容/不兼容/需过渡 三种情况正确判断
      [OK] frequency 差 >4x 产生 warning
      [OK] action_space_type 不同产生 warning
      [OK] precondition=any 始终兼容
      [OK] 多节点链验证正确
      [OK] 空链/单节点边界情况正确

  Task A3: TransitionLibrary                                         [DONE]
    操作:
      - CREATE src/system/skill/transition_library.py
        内置过渡: any→standing, standing→walking_ready, walking→standing
        注册模式: register_transition(from_posture, to_posture, bundle_path)
      - CREATE custom_mods/training/transitions/ (+.gitkeep)
    验收:
      [OK] 3个内置过渡已注册 (any→standing, standing→walking_ready, walking→standing)
      [OK] lookup_transition 精确匹配 + wildcard "any" 回退
      [OK] 自定义过渡目录 custom_mods/training/transitions/.gitkeep 存在
      [OK] 现有测试不回归 (167 passed, 1 pre-existing failure)

══════════════════════════════════════════════════════
Phase 3: Training Bridge (B1, B2)
  依赖: Phase 1
  门控: SB3 trainer 产出的 bundle 包含有效 SkillManifest v2
══════════════════════════════════════════════════════

  Task B1: TrainingSpec → SkillManifest bridge                       [DONE]
    操作:
      - MODIFY src/system/training/training_spec.py
        新增 TrainingJobSpec.derive_skill_manifest_fields() 方法
        从 robot_family, obs_action_config, physics_config, algorithm_config 推导
      - MODIFY src/system/training/bundle_exporter.py
        build_manifest() 末尾注入 skill section (v2)
      - MODIFY src/system/training/task_templates.py
        所有 5 个 family template 增加 precondition_posture / postcondition_posture
    验收:
      [OK] derive_skill_manifest_fields() 输出完整: source_type, action_space_type,
           target_robot_family, pre/postcondition, sensors, training_source
      [OK] task_templates 所有 5 个模板有 pre/postcondition
      [OK] build_manifest() 输出包含 skill section

  Task B2: Bundle export manifest v2                                 [DONE]
    操作:
      - MODIFY src/system/training/bundle_exporter.py
        manifest.yaml v2 格式: v1 字段不变 + 新增 skill section
      - MODIFY src/system/service/checkpoint_registry.py
        CheckpointEntry 新增 skill_manifest 字段
        _parse_entry() 加载时调用 load_skill_manifest() 自动 v1→v2 迁移
    验收:
      [OK] PPO_WALK/SAC_WALK v1 bundle 加载后 skill_manifest 不为 None
      [OK] v1 迁移结果: precondition=any, postcondition=standing, tags=[v1_migrated]
      [OK] 现有测试不回归 (198 passed, 13+1 pre-existing failures)

══════════════════════════════════════════════════════
Phase 4: Import Bridge (B3)
  依赖: Phase 1 + B2
  门控: HF 导入的 bundle 有自动推断的 SkillManifest
══════════════════════════════════════════════════════

  Task B3: HuggingFace import manifest inference                     [DONE]
    操作:
      - CREATE src/system/skill/manifest_inferrer.py
        从 ONNX 模型 input/output shape 自动推断
      - MODIFY src/system/training/hf_downloader.py
        导入时调用推断器
      - MODIFY bin/pages/training/checkpoint_import_dialog.py
        显示 manifest 字段供审查/编辑
      - MODIFY src/system/service/checkpoint_registry.py
        peek_manifest() 扩展返回 skill section 字段
      - MODIFY src/system/skill/__init__.py
        导出 infer_skill_manifest_fields
    验收:
      [OK] ONNX 模型可自动推断 action_dim, observation_dim (via onnxruntime shape inspection)
      [OK] 缺失关键字段时通过 heuristics + 已知机器人配置补全默认值
      [OK] 导入对话框显示可编辑 SkillManifest 字段 (action_space_type, frequency, robot_family, pre/postcondition)
      [OK] 用户编辑的字段在 Import 前写回 manifest.yaml
      [OK] peek_manifest() 返回 skill section 字段供 preview UI 使用
      [OK] py_compile 通过 (5/5 文件)
      [OK] 现有测试不回归 (pre-existing failures unchanged)

══════════════════════════════════════════════════════
Phase 5: Runtime Integration (A4, C1)
  依赖: Phase 1 + Phase 2
  门控: RuntimeEngine 执行 DAG 时有过渡验证和统一分发
══════════════════════════════════════════════════════

  Task A4: DAG pre-flight check integration                          [DONE]
    操作:
      - MODIFY src/system/engine/contracts.py
        新增 DiagnosticsKey.TRANSITION_VALIDATION_REPORT,
        TRANSITION_INSERTED_NODES, TRANSITION_VALIDATION_STATUS
      - MODIFY src/system/core/error_ux.py
        新增 transition_blocked, transition_velocity_mismatch 错误码 + operator text + categories
      - MODIFY src/system/engine/runtime_engine.py
        execute() 中安全检查后运行 _run_transition_preflight()
        不兼容时 blocked(BLOCKED_COMPILE, "transition_blocked")
        validation report 附加到 result.diagnostics
    验收:
      [OK] 不兼容的 skill 链被拦截 (BLOCKED_COMPILE + transition_blocked)
      [OK] 可过渡的间隙记录 insertions 到 diagnostics
      [OK] 3 个 TRANSITION_* DiagnosticsKey 常量已添加
      [OK] 错误码和 category 已注册
      [OK] py_compile 通过 (3/3 文件)
      [OK] 现有测试不回归

  Task C1: Unified ActionDispatcher                                  [DONE]
    操作:
      - CREATE src/system/runtime/__init__.py
      - CREATE src/system/runtime/action_dispatcher.py
        ActionDispatcher: dispatch(binding_output, context) → ExecutorResult
        dispatch_policy_action(action, skill_manifest, context) → ExecutorResult
        executor registry: brand+backend → concrete executor (lazy init)
        action_range 钳制, robot_type 提取
      - MODIFY src/system/policy/action_applier.py
        新增 dispatch_to_backend() 方法: apply() + ActionDispatcher 委托
    验收:
      [OK] sim 和 real 通过同一 ActionDispatcher.dispatch() 路径
      [OK] action_space_type 通过 BindingOutput.operation 正确路由
      [OK] brand+backend executor 解析 (unitree/spot + sdk/mujoco)
      [OK] py_compile 通过 (3/3 文件)
      [OK] 现有测试不回归

══════════════════════════════════════════════════════
Phase 6: Interaction (C2, C3, C4)
  依赖: C1
  门控: 用户可通过键盘在 MuJoCo sim 中控制机器人，使用同一分发路径
══════════════════════════════════════════════════════

  Task C2: UserInputManager                                          [DONE]
    操作:
      - CREATE src/system/runtime/user_input_manager.py
        UserInputManager: key_pressed/released, get_velocity_command(), get_action()
        内置 keyboard_quadruped + gamepad_quadruped profiles
        支持 WASD+QE 速度控制, Shift 加速, Space 急停
      - CREATE src/system/runtime/input_profiles/quadruped_keyboard.yaml
      - CREATE src/system/runtime/input_profiles/quadruped_gamepad.yaml
      - CREATE src/system/nodes/sys_nodes/manual_control_node.py
        ManualControlNode(BaseNode): mode/profile/max_duration/command_scale 参数
        已注册到 sys_nodes/__init__.py __all__
      - MODIFY bin/pages/layout/ui.py
        keyPressEvent/keyReleaseEvent 转发到 _user_input_manager
        set_user_input_manager() 绑定/解绑方法
    验收:
      [OK] WASD/手柄 → 标准动作格式 (np.ndarray, same as policy output)
      [OK] ManualControlNode 在 sys_nodes 模块中可导入
      [OK] py_compile 通过 (4/4 文件)

  Task C3: MuJoCo sim execution completeness                         [DONE]
    操作:
      - CREATE src/system/sim/__init__.py
      - CREATE src/system/sim/sensor_manager.py
        SensorManager: configure(required_sensors), read_all() → Dict
        5 种传感器读取器: imu, joint_encoder, rgb_camera, depth_camera, force_torque
        动态激活/停用, offscreen renderer 延迟创建
      - MODIFY src/system/policy/sim_env_context.py
        新增 sensor_manager, overlay_info 字段
        configure_sensors(), get_sensor_readings(), set_overlay() 方法
      - MODIFY src/system/engine/simulation_runner.py
        新增 overlay_info dict + overlay_updated Signal + set_overlay()
    验收:
      [OK] required_sensors 全部有 MuJoCo 读取实现 (5/5 传感器类型)
      [OK] 行为切换时通过 configure() 动态激活/停用
      [OK] viewer overlay 信息可通过 Signal 传递给 UI
      [OK] py_compile 通过 (4/4 文件)

  Task C4: SDK adapter SkillManifest alignment                       [DONE]
    操作:
      - MODIFY src/system/service/adapters/base_adapter.py
        新增 accept_action(action, skill_manifest) → Dict
        新增 report_sensors(required_sensors) → Dict
        新增 check_precondition(precondition) → bool
        全部非抽象, 安全默认 (ok=True / True / 委托 get_sensor_data)
      - MODIFY unitree_sdk2/adapter.py
        accept_action: joint_position/torque → set_joint_command, velocity → velocity_move
        report_sensors: sensor_key_map 映射 imu/joint_encoder/force_torque
        check_precondition: posture=any 直接通过, 其他检查 model 连接
      - MODIFY spot_sdk/adapter.py
        accept_action: session guard, command client routing
        report_sensors: 委托 get_sensor_data 过滤
        check_precondition: session + e-stop 检查
      - MODIFY cyberdog_sdk/adapter.py
        accept_action: session guard, velocity publisher routing
        report_sensors: 委托 get_sensor_data 过滤
        check_precondition: session 检查
    验收:
      [OK] 三个适配器实现新接口 (accept_action, report_sensors, check_precondition)
      [OK] semantic_actions 保留用于非策略行为 (run_action 不变)
      [OK] py_compile 通过 (4/4 文件)
      [OK] 现有测试不回归

══════════════════════════════════════════════════════
Phase 7: Canvas UI (A5)
  依赖: A4
  门控: Canvas 显示 SkillManifest 徽章和过渡节点
══════════════════════════════════════════════════════

  Task A5: Canvas SkillNode upgrade                                  [DONE]
    操作:
      - MODIFY src/system/nodes/sys_nodes/behavior_node.py
        新增 skill_manifest_summary() 方法: 从 CheckpointRegistry 加载 SkillManifest
        返回 action_space_type, frequency, pre/postcondition, robot_family, backend
      - MODIFY bin/pages/canvas/graph_scene.py
        Behavior 节点新增 manifest badge row:
          action_space_type 彩色徽章 (JP/JT/CD/EE/HY + 颜色编码)
          control_frequency_hz 标签
          pre→post posture 文本
          兼容状态圆形徽章 (✓ green / — gray)
        _on_behavior_change 回调自动更新 badge
        _resolve_node_gradient 新增 transition/manual_control 颜色映射
      - MODIFY bin/nodes/training_node_items.py
        paint() 标题栏右侧新增 SkillManifest badge 渲染
        _get_manifest_badge_text() 从 logic_node 获取 manifest summary
        5 种 action_space_type 颜色: JP=蓝, JT=红, CD=紫, EE=橙, HY=绿
    验收:
      [OK] BehaviorNode 显示 manifest 摘要 (action badge + freq + posture + compat)
      [OK] 过渡节点在画布中使用 sensor 颜色方案视觉区分
      [OK] Training Ground 节点标题栏显示 action_space_type 小徽章
      [OK] py_compile 通过 (3/3 文件)
      [OK] 现有测试不回归 (172 passed, 11 pre-existing failures)

══════════════════════════════════════════════════════
不变量 (每次提交必须保持):
  - 现有测试通过 (已知 12 GUI + 9 SDK 缺失失败除外)
  - Node Registry 模式不变 (register_node/get_node_class)
  - RuntimeResult 契约不变
  - GraphScene 序列化向后兼容
  - Mission schema v1.4 文件可加载
  - IR 往返 (Canvas ↔ IR ↔ Code) 不受影响
  - 无 SkillManifest v2 字段的旧 bundle 仍可加载

不可触碰:
  - compiler/*
  - system/behavior/*
  - bin/pages/canvas/code_editor.py
  - nodes/__init__.py
