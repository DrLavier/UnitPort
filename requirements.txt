# UnitPort - Robot Visual Programming Platform
# Python dependency list

# Core runtime dependencies
PySide6>=6.5.0
mujoco>=3.0.0
numpy>=1.24.0
PyYAML>=6.0
onnxruntime>=1.17
huggingface_hub==1.7.1
psutil>=5.9

# Training stack (RL algorithms + environment API)
# gymnasium is pulled in automatically as an SB3 dependency
stable_baselines3>=2.0.0

# Note: torch is installed separately by install.bat
# to select the correct CPU or CUDA build for the current hardware.
# Minimum required: torch>=2.0.0

# Optional: Unitree SDK2
# The project imports `unitree_sdk2py`, but this is typically installed from
# Unitree's official SDK distribution (local/vendor path), not PyPI.
# unitree_sdk2py
