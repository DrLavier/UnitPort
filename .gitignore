# Python venv (dev only)
venv/
.venv311/
Engines/

# DEV Tools
.claude/
CLAUDE.md
.examples/


# Local-only development notes: planning, audit evidence, design WIP,
# Claude Code working set. Source code must not reference anything in
# here; release packaging excludes the directory explicitly (see
# application/service/updater/channels/source_zip.py).
knowledge_base/

# pip temp + cache (redirected by install.bat/install.sh onto the project drive)
.pip-tmp/
.pip-cache/

# ── Runtime payload (generated by scripts/build_runtime.bat) ──────────────
/runtime/
# ── Runtime cache + env state (icons, install_state.json, ...) ────────────
/src/runtime/

# Distribution output
dist_package/
/projects/
*.zip

# Byte-compiled / cache
__pycache__/
*.py[cod]
*.pyo
*.claude

# Build / packaging
build/
dist/
*.egg-info/

# Logs / runtime — Paths.LOGS_DIR resolves to ./log/ (singular)
log/
*.log

# Temp / editor
*.tmp
.tmp/
*.swp
*.swo
*.bak
*.orig
.cache/
.pytest_cache/
.idea/
.vscode/

# OS files
.DS_Store
desktop.ini
Thumbs.db

# Project-level ignores
AGENTS.md

/tools/
custom_mods/models/
