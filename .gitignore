# Python venv (dev only)
venv/
.venv311/

# ── Runtime payload (generated by scripts/build_runtime.bat) ──────────────
# These are binary/large assets that must NOT be committed.
# Maintainers generate them locally via scripts/build_runtime.bat.
runtime/python/
runtime/cyclonedds/
runtime/wheels/*.whl
runtime/wheels/*.tar.gz
runtime/wheels/*.zip
runtime/_downloads/

# Runtime state files (machine-local, written at install/run time)
runtime/env/install_state.json
runtime/env/bootstrap_state.json

# Distribution output
dist_package/
projects/
*.zip

# Byte-compiled / cache
__pycache__/
__cache__
*.claude
*.py[cod]
*.pyo
./claude
*.claude

# Build / packaging
build/
dist/
*.egg-info/

# Logs / runtime
logs/
*.log

# Temp / editor
*.tmp
.tmp/
*.swp
*.swo
*.bak
.cache/
.pytest_cache/

# OS files
.DS_Store
desktop.ini
AGENTS.md

# MuJoCo Menagerie — auto-downloaded at startup, not bundled in repo
models/mujoco_menagerie/

# Local config overrides
config/user.ini
config/*.local.ini
config/local.ini

# Policy weight binaries — do not commit large model files
custom_weights/**/*.onnx
custom_weights/**/*.pt
custom_weights/**/*.safetensors
custom_weights/**/*.bin
